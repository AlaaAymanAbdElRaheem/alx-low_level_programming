#include "main.h"
#include <stdio.h>

/**
 * mul - multiplies two numbers
 * @a: first number
 * @b: second number
 * Return: 0
 */

int mul(int a, int b)
{
	int multi;

	multi = a * b;
	printf("%d", multi);
}
