#include "main.h"

/**
 * *_strchr - gets the first occurrence of the character c in the string s
 * @s: string
 * @c: character
 * Return: s or null
 */

char *_strchr(char *s, char c)
{
	while (*s != '\0')
	{
		if (*s == c)
			return (s);
		s++
	}
	if (c == '\0')
		return (s);
	return (0);
}
